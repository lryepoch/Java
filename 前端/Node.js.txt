node.js：

--基本信息：
	1.运行在服务器端的js
	2.加载模块，使用模块的方法。
	3.npm是node.js的包管理器，可以上传或者下载。使用 npm 命令安装模块。
	4.模块放在node_modules 目录中，因此在代码中只需要通过 require('express') 的方式就好，无需指定第三方包路径。 
	5.使用 package.json：package.json 位于模块的目录下，用于定义包的属性。看下 express 包的 package.json 文件，位于 node_modules/express/package.json 内容。

